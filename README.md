# primero tenemos que crear un ambiente virtual, para ello corremos en terminal lo siguiente:

### python3 -m venv venv
### source venv/bin/activate

### python3 -m pip install --upgrade pip
### python3 -m pip install "setuptools<70" "wheel<0.45"

### pip install -r requirements.txt

### mkdir mlruns

### prueba
### prueba local
### prueba remota 2
### prueba local 2